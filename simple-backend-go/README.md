# Simple backend on Go

Тот же самый минимальный backend, что и в Python-версии, но на Go 1.23 и Gin.

## Эндпоинты

- `POST /api/favorites`
- `GET /api/favorites`

## Локальный запуск

```bash
go run .
```

После этого сервис будет доступен на `http://localhost:8080`.
